# CST-405
This is the repository for CST-405 compilers

Running the Compiler:
1. type 'make' into the console
2. type ./parser test.c output.s into the console
3. type spim -file output.s into the console